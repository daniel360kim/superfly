# Python caches / build
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual environments (repo root = agile venv; methods/<repo>/.venv per method)
.venv/
venv/
env/

# Comparison harness outputs (regenerated each run; also holds mesh_cache/,
# the scene-mesh clearance cache -- large .npz, re-extracted on demand)
/results/

# Sim / camera debug artifacts (regenerated each run, written to scripts/
# because the harness launches with cwd=scripts/)
scripts/camera_debug.png
scripts/depth_debug.npy
scripts/agile_overhead_debug.png
scripts/*.mp4

# Editor / OS
.DS_Store
.idea/
.vscode/

# Hydra training run outputs / logs (regenerated per run)
outputs/
train.log
